rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Songs collection - allow read and write for all (for development)
    match /songs/{songId} {
      allow read, write: if true;
    }
  }
}
