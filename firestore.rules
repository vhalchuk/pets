rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Songs collection - allow read for all, write requires authentication
    match /songs/{songId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
